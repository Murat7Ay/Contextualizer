<Page x:Class="WpfInteractionApp.Pages.HandlersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="Handlers">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Handlers"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Click="ManageHandlers_Click">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="Settings24" />
                    </ui:Button.Icon>
                    Manage
                </ui:Button>

                <ui:Button Click="Marketplace_Click" Margin="8,0,0,0">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="Cart24" />
                    </ui:Button.Icon>
                    Marketplace
                </ui:Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,16,0,0">
            <ui:Card x:Name="EmptyStateCard" Padding="24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <ui:SymbolIcon Symbol="ClipboardTask24" FontSize="28" Margin="0,0,10,0" />
                        <TextBlock Text="Henüz açık bir handler ekranı yok."
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                    </StackPanel>

                    <TextBlock Text="Bir handler 'show_window' ile ekran açtığında burada sekme olarak görünecek."
                               Opacity="0.8"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,16" />

                    <StackPanel Orientation="Horizontal">
                        <ui:Button Click="ManualHandlers_Click">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Flash24" />
                            </ui:Button.Icon>
                            Manual handlers
                        </ui:Button>

                        <ui:Button Click="Marketplace_Click" Margin="8,0,0,0">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Cart24" />
                            </ui:Button.Icon>
                            Explore Marketplace
                        </ui:Button>
                    </StackPanel>
                </StackPanel>
            </ui:Card>

            <TabControl x:Name="TabControl"
                        Visibility="Collapsed"
                        Style="{DynamicResource Carbon.TabControl}">
                <TabControl.ItemContainerStyle>
                    <Style TargetType="TabItem" BasedOn="{StaticResource Carbon.TabItem}" />
                </TabControl.ItemContainerStyle>
            </TabControl>
        </Grid>
    </Grid>
</Page>


