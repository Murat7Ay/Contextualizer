<Window x:Class="WpfInteractionApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WPF Interaction App" Height="600" Width="1000"
        Background="#161616" Foreground="#F4F4F4">
    <Window.Resources>
        <!-- Carbon Design System Stilleri -->
        <Style x:Key="CarbonTextBlock" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="#F4F4F4" />
        </Style>
        <Style x:Key="CarbonListBox" TargetType="ListBox">
            <Setter Property="Background" Value="#262626" />
            <Setter Property="BorderBrush" Value="#393939" />
            <Setter Property="Foreground" Value="#F4F4F4" />
        </Style>
        <Style x:Key="CarbonTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="#161616" />
            <Setter Property="BorderBrush" Value="#393939" />
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#F4F4F4" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Margin" Value="5,0,0,0" />
            <Setter Property="Content" Value="×" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="ExtraBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#525252" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="100" /> <!-- Log alanı -->
            <ColumnDefinition Width="5" />                  <!-- GridSplitter için -->
            <ColumnDefinition Width="*" />                  <!-- Tab alanı -->
        </Grid.ColumnDefinitions>

        <!-- Log Listesi -->
        <ListBox Grid.Column="0" x:Name="LogListBox" Style="{StaticResource CarbonListBox}" Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:MM.dd HH:mm}'}" Style="{StaticResource CarbonTextBlock}" />
                        <TextBlock Text="{Binding Message}" Style="{StaticResource CarbonTextBlock}" />
                        <TextBlock Text="{Binding AdditionalInfo}" Style="{StaticResource CarbonTextBlock}" FontStyle="Italic" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- GridSplitter -->
        <GridSplitter Grid.Column="1"
                      Width="5"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="#393939"
                      ShowsPreview="True" />

        <!-- Sekme Alanı -->
        <TabControl Grid.Column="2" x:Name="TabControl" Style="{StaticResource CarbonTabControl}" Margin="10"/>
    </Grid>
</Window>