<Window x:Class="WpfInteractionApp.HandlerExchangeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:WpfInteractionApp.Converters"
        Title="Handler Exchange" Height="600" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Carbon.Brush.Background.Primary}"
        Foreground="{DynamicResource Carbon.Brush.Text.Primary}"
        Style="{DynamicResource Carbon.Window}">
    
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:ArrayToVisibilityConverter x:Key="ArrayToVisibilityConverter"/>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="20" />
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"
                                                                Margin="6,3"
                                                                HorizontalAlignment="Left"
                                                                VerticalAlignment="Center" />
                                                <Path x:Name="Arrow"
                                                      Grid.Column="1"
                                                      Fill="{DynamicResource Carbon.Brush.Text.Primary}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Data="M0,0 L4,4 L8,0 Z"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            Margin="6,3"
                                            IsHitTestVisible="False"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                            <Popup x:Name="Popup"
                                   AllowsTransparency="True"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                      SnapsToDevicePixels="True">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource Carbon.Brush.Background.Secondary}"
                                            BorderBrush="{DynamicResource Carbon.Brush.Background.Tertiary}"
                                            BorderThickness="1"/>
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                Padding="6,3">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="ListView">
            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
            <Setter Property="Padding" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Arama ve Filtreleme -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchBox" Width="200" Margin="0,0,10,0" 
                     TextChanged="SearchBox_TextChanged"
                     ToolTip="Search in handler name or description"/>
            <ComboBox x:Name="TagFilter" Width="150" Margin="0,0,10,0"
                      SelectionChanged="TagFilter_SelectionChanged"
                      ToolTip="Filter by tag"/>
            <Button Content="Refresh" Click="Refresh_Click" Margin="0,0,10,0"
                    Style="{DynamicResource Carbon.Button.Secondary}"/>
            <Button Content="Add New Handler" Click="AddHandler_Click"
                    Style="{DynamicResource Carbon.Button.Secondary}"/>
        </StackPanel>

        <!-- Sıralama -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Sort by:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="SortComboBox" Width="150" SelectionChanged="SortComboBox_SelectionChanged">
                <ComboBoxItem Content="Name (A-Z)" IsSelected="True"/>
                <ComboBoxItem Content="Name (Z-A)"/>
                <ComboBoxItem Content="Newest"/>
                <ComboBoxItem Content="Most Downloaded"/>
            </ComboBox>
        </StackPanel>

        <!-- ✨ Card-based Handler List -->
        <ScrollViewer Grid.Row="2" Margin="0,0,0,10" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="HandlersItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{DynamicResource Carbon.Card.Hover}" Margin="0,0,0,16" MouseLeftButtonDown="HandlerCard_Click">
                            <Grid Margin="20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Handler Info -->
                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="18" 
                                                  Style="{DynamicResource Carbon.TextBlock.Base}"/>
                                        
                                        <!-- Status Badges -->
                                        <Border Style="{DynamicResource Carbon.Badge.Success}" Margin="12,0,0,0"
                                               Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <TextBlock Text="INSTALLED" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                        
                                        <Border Style="{DynamicResource Carbon.Badge.Warning}" Margin="6,0,0,0"
                                               Visibility="{Binding HasUpdate, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <TextBlock Text="UPDATE AVAILABLE" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                    </StackPanel>
                                    
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" 
                                              Style="{DynamicResource Carbon.TextBlock.Body}" 
                                              Opacity="0.8" Margin="0,0,0,12"/>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="v" FontSize="12" Opacity="0.6" 
                                                  Style="{DynamicResource Carbon.TextBlock.Base}"/>
                                        <TextBlock Text="{Binding Version}" FontSize="12" Opacity="0.6" 
                                                  Margin="2,0,16,0" Style="{DynamicResource Carbon.TextBlock.Base}"/>
                                        <TextBlock Text="by" FontSize="12" Opacity="0.6" 
                                                  Style="{DynamicResource Carbon.TextBlock.Base}"/>
                                        <TextBlock Text="{Binding Author}" FontSize="12" Opacity="0.6" 
                                                  Margin="4,0,16,0" Style="{DynamicResource Carbon.TextBlock.Base}"/>
                                        <TextBlock Text="Downloads:" FontSize="12" Opacity="0.6" 
                                                  Style="{DynamicResource Carbon.TextBlock.Base}"/>
                                        <TextBlock Text="{Binding DownloadCount}" FontSize="12" Opacity="0.6" 
                                                  Margin="4,0,0,0" Style="{DynamicResource Carbon.TextBlock.Base}"/>
                                    </StackPanel>
                                    
                                    <!-- Tags -->
                                    <ItemsControl ItemsSource="{Binding Tags}" Margin="0,0,0,8">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#E5E7EB" CornerRadius="6" Padding="6,3" Margin="0,0,6,4">
                                                    <TextBlock Text="{Binding}" FontSize="10" Foreground="#374151" FontWeight="Medium"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    
                                    <!-- Dependencies -->
                                    <StackPanel Orientation="Horizontal" Visibility="{Binding Dependencies, Converter={StaticResource ArrayToVisibilityConverter}}">
                                        <TextBlock Text="Dependencies:" FontSize="11" Opacity="0.6" 
                                                  Style="{DynamicResource Carbon.TextBlock.Base}" Margin="0,0,8,0"/>
                                        <ItemsControl ItemsSource="{Binding Dependencies}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}" FontSize="11" Opacity="0.6" 
                                                              Style="{DynamicResource Carbon.TextBlock.Base}" Margin="0,0,8,0"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </StackPanel>
                                
                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" MinWidth="120">
                                    <Button Content="Details" Style="{DynamicResource Carbon.Button.Secondary}" 
                                           Margin="0,0,0,8" HorizontalAlignment="Stretch" Click="Details_Click"/>
                                    
                                    <Button Content="Install" Style="{DynamicResource Carbon.Button.Primary}"
                                           Visibility="{Binding IsInstalled, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                           Margin="0,0,0,8" HorizontalAlignment="Stretch" Click="Install_Click"/>
                                    
                                    <Button Content="Update" Style="{DynamicResource Carbon.Button.Warning}"
                                           Visibility="{Binding HasUpdate, Converter={StaticResource BoolToVisibilityConverter}}"
                                           Margin="0,0,0,8" HorizontalAlignment="Stretch" Click="Update_Click"/>
                                    
                                    <Button Content="Remove" Style="{DynamicResource Carbon.Button.Danger}"
                                           Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVisibilityConverter}}"
                                           HorizontalAlignment="Stretch" Click="Remove_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Alt Butonlar -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button Content="Close" 
                    Width="100" 
                    Click="Close_Click"
                    Style="{DynamicResource Carbon.Button.Base}"/>
        </StackPanel>
    </Grid>
</Window> 