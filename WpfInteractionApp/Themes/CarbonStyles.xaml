<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Additional Colors -->
    <Color x:Key="Carbon.Color.Background.Active">#393939</Color>
    <Color x:Key="Carbon.Color.Text.Helper">#8D8D8D</Color>
    <Color x:Key="Carbon.Color.Interactive.Active">#393939</Color>
    <Color x:Key="Carbon.Color.Interactive.Disabled">#393939</Color>

    <!-- Additional Brushes -->
    <SolidColorBrush x:Key="Carbon.Brush.Background.Active" Color="{StaticResource Carbon.Color.Background.Active}"/>
    <SolidColorBrush x:Key="Carbon.Brush.Text.Helper" Color="{StaticResource Carbon.Color.Text.Helper}"/>
    <SolidColorBrush x:Key="Carbon.Brush.Interactive.Active" Color="{StaticResource Carbon.Color.Interactive.Active}"/>
    <SolidColorBrush x:Key="Carbon.Brush.Interactive.Disabled" Color="{StaticResource Carbon.Color.Interactive.Disabled}"/>

    <!-- Shadow Effects -->
    <DropShadowEffect x:Key="Carbon.Shadow.Small" 
                      ShadowDepth="1" 
                      Direction="270" 
                      Color="#40000000" 
                      BlurRadius="3" 
                      Opacity="0.2"/>
    
    <DropShadowEffect x:Key="Carbon.Shadow.Medium" 
                      ShadowDepth="2" 
                      Direction="270" 
                      Color="#40000000" 
                      BlurRadius="6" 
                      Opacity="0.3"/>
    
    <DropShadowEffect x:Key="Carbon.Shadow.Large" 
                      ShadowDepth="4" 
                      Direction="270" 
                      Color="#40000000" 
                      BlurRadius="12" 
                      Opacity="0.4"/>

    <!-- Base Text Styles -->
    <Style x:Key="Carbon.TextBlock.Base" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="1.5" />
    </Style>

    <Style x:Key="Carbon.TextBlock.Heading" TargetType="TextBlock" BasedOn="{StaticResource Carbon.TextBlock.Base}">
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,16" />
        <Setter Property="LineHeight" Value="1.3" />
    </Style>

    <Style x:Key="Carbon.TextBlock.Subheading" TargetType="TextBlock" BasedOn="{StaticResource Carbon.TextBlock.Base}">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Margin" Value="0,0,0,12" />
    </Style>

    <Style x:Key="Carbon.TextBlock.Body" TargetType="TextBlock" BasedOn="{StaticResource Carbon.TextBlock.Base}">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="LineHeight" Value="20" />
        <Setter Property="Margin" Value="0,0,0,8" />
    </Style>

    <Style x:Key="Carbon.TextBlock.Helper" TargetType="TextBlock" BasedOn="{StaticResource Carbon.TextBlock.Base}">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{StaticResource Carbon.Brush.Text.Helper}" />
        <Setter Property="Margin" Value="0,4,0,0" />
    </Style>

    <!-- Button Styles -->
    <Style x:Key="Carbon.Button.Base" TargetType="Button">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="16,10" />
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinWidth" Value="100" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}">
                            <ContentPresenter.Effect>
                                <DropShadowEffect ShadowDepth="1" 
                                                Direction="270" 
                                                Color="#40000000" 
                                                BlurRadius="4" 
                                                Opacity="0.3"/>
                            </ContentPresenter.Effect>
                        </ContentPresenter>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Hover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Active}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Disabled}" />
                            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Helper}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Carbon.Button.Secondary" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Base}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />

        <Style.Triggers>
            <!-- Mouse üzerine gelindiğinde -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
                <Setter Property="Foreground" Value="#FFFFFF" />
            </Trigger>

            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Active}" />
                <Setter Property="Foreground" Value="#FFFFFF" />
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Interactive.Disabled}" />
                <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Interactive.Disabled}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Light Theme Button Styles -->
    <Style x:Key="Carbon.Button.Light" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Base}">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}">
                            <ContentPresenter.Effect>
                                <DropShadowEffect ShadowDepth="1" 
                                                Direction="270" 
                                                Color="#40000000" 
                                                BlurRadius="4" 
                                                Opacity="0.2"/>
                            </ContentPresenter.Effect>
                        </ContentPresenter>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Hover}" />
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="2" 
                                                    Direction="270" 
                                                    Color="#40000000" 
                                                    BlurRadius="6" 
                                                    Opacity="0.3"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Active}" />
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="0" 
                                                    Direction="270" 
                                                    Color="#40000000" 
                                                    BlurRadius="0" 
                                                    Opacity="0"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Disabled}" />
                            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Helper}" />
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="0" 
                                                    Direction="270" 
                                                    Color="#40000000" 
                                                    BlurRadius="0" 
                                                    Opacity="0"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Carbon.Button.Light.Secondary" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Light}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
                <Setter Property="Foreground" Value="#FFFFFF" />
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="2" 
                                        Direction="270" 
                                        Color="#40000000" 
                                        BlurRadius="6" 
                                        Opacity="0.3"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Active}" />
                <Setter Property="Foreground" Value="#FFFFFF" />
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="0" 
                                        Direction="270" 
                                        Color="#40000000" 
                                        BlurRadius="0" 
                                        Opacity="0"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Interactive.Disabled}" />
                <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Interactive.Disabled}" />
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="0" 
                                        Direction="270" 
                                        Color="#40000000" 
                                        BlurRadius="0" 
                                        Opacity="0"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Dark Theme Button Styles -->
    <Style x:Key="Carbon.Button.Dark" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Base}">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}">
                            <ContentPresenter.Effect>
                                <DropShadowEffect ShadowDepth="1" 
                                                Direction="270" 
                                                Color="#40000000" 
                                                BlurRadius="4" 
                                                Opacity="0.3"/>
                            </ContentPresenter.Effect>
                        </ContentPresenter>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Hover}" />
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="2" 
                                                    Direction="270" 
                                                    Color="#40000000" 
                                                    BlurRadius="6" 
                                                    Opacity="0.4"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Active}" />
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="0" 
                                                    Direction="270" 
                                                    Color="#40000000" 
                                                    BlurRadius="0" 
                                                    Opacity="0"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Disabled}" />
                            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Helper}" />
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="0" 
                                                    Direction="270" 
                                                    Color="#40000000" 
                                                    BlurRadius="0" 
                                                    Opacity="0"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Carbon.Button.Dark.Secondary" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Dark}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
                <Setter Property="Foreground" Value="#FFFFFF" />
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="2" 
                                        Direction="270" 
                                        Color="#40000000" 
                                        BlurRadius="6" 
                                        Opacity="0.4"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Active}" />
                <Setter Property="Foreground" Value="#FFFFFF" />
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="0" 
                                        Direction="270" 
                                        Color="#40000000" 
                                        BlurRadius="0" 
                                        Opacity="0"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Interactive.Disabled}" />
                <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Interactive.Disabled}" />
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="0" 
                                        Direction="270" 
                                        Color="#40000000" 
                                        BlurRadius="0" 
                                        Opacity="0"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Dim Theme Button Styles -->
    <Style x:Key="Carbon.Button.Dim" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Base}">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Hover}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Active}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Carbon.Button.Dim.Secondary" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Dim}">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Secondary}" />
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Interactive.Primary}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Active}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ListBox Styles -->
    <Style x:Key="Carbon.ListBox" TargetType="ListBox">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ScrollViewer Padding="{TemplateBinding Padding}"
                                    Focusable="False">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ListBoxItem Style -->
    <Style x:Key="Carbon.ListBoxItem" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="4">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource Carbon.Brush.Interactive.Hover}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource Carbon.Brush.Interactive.Active}" />
                            <Setter Property="TextElement.Foreground" Value="{StaticResource Carbon.Brush.Text.Primary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabControl Styles -->
    <Style x:Key="Carbon.TabControl" TargetType="TabControl">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" 
                                Background="{StaticResource Carbon.Brush.Background.Secondary}"
                                BorderThickness="0,0,0,1"
                                BorderBrush="{StaticResource Carbon.Brush.Background.Tertiary}">
                            <TabPanel IsItemsHost="True" />
                        </Border>
                        <Border Grid.Row="1" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter ContentSource="SelectedContent" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Carbon.TabItem" TargetType="TabItem">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Secondary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Margin" Value="2,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="6,6,0,0"
                            Margin="{TemplateBinding Margin}">
                        <ContentPresenter ContentSource="Header"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Carbon.Brush.Background.Primary}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}" />
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- GridSplitter Style -->
    <Style x:Key="Carbon.GridSplitter" TargetType="GridSplitter">
        <Setter Property="Background" Value="{StaticResource Carbon.Brush.Background.Tertiary}" />
        <Setter Property="Width" Value="4" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Border Background="{TemplateBinding Background}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource Carbon.Brush.Interactive.Primary}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Close Button Style -->
    <Style x:Key="CloseButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="4,0,0,0"/>
        <Setter Property="ToolTip" Value="Close"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="4">
                        <Path x:Name="ClosePath"
                              Data="M 0,0 L 8,8 M 0,8 L 8,0"
                              Stroke="{DynamicResource Carbon.Brush.Text.Primary}"
                              StrokeThickness="2"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Opacity="0.7"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Active}"/>
                            <Setter TargetName="ClosePath" Property="Opacity" Value="1"/>
                            <Setter TargetName="ClosePath" Property="Stroke" Value="{DynamicResource Carbon.Brush.Danger.Primary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ScrollViewer Style -->
    <Style x:Key="Carbon.ScrollViewer" TargetType="ScrollViewer">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>

                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                 Grid.Column="1" Grid.Row="0"
                                 Value="{TemplateBinding VerticalOffset}"
                                 Maximum="{TemplateBinding ScrollableHeight}"
                                 ViewportSize="{TemplateBinding ViewportHeight}"
                                 Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                                 Orientation="Horizontal"
                                 Grid.Column="0" Grid.Row="1"
                                 Value="{TemplateBinding HorizontalOffset}"
                                 Maximum="{TemplateBinding ScrollableWidth}"
                                 ViewportSize="{TemplateBinding ViewportWidth}"
                                 Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ScrollBar Style -->
    <Style x:Key="Carbon.ScrollBar" TargetType="ScrollBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Border Background="{StaticResource Carbon.Brush.Background.Secondary}"
                            BorderThickness="0">
                        <Track x:Name="PART_Track" 
                               IsDirectionReversed="True"
                               Width="8">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand" Style="{StaticResource Carbon.ScrollBarButton}"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand" Style="{StaticResource Carbon.ScrollBarButton}"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource Carbon.ScrollBarThumb}"/>
                            </Track.Thumb>
                        </Track>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ScrollBar Button Style -->
    <Style x:Key="Carbon.ScrollBarButton" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="{TemplateBinding Background}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ScrollBar Thumb Style -->
    <Style x:Key="Carbon.ScrollBarThumb" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="{StaticResource Carbon.Brush.Background.Active}"
                            CornerRadius="4"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menu Styles -->
    <Style x:Key="Carbon.Menu" TargetType="Menu">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}" />
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}" />
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Menu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource Carbon.Brush.Background.Tertiary}"
                            BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal"
                                  IsItemsHost="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Base MenuItem Style -->
    <Style x:Key="Carbon.MenuItem.Base" TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="Border" 
                            Background="{TemplateBinding Background}"
                            BorderThickness="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                <ColumnDefinition Width="13"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="Icon" 
                                            Margin="6,0,6,0" 
                                            VerticalAlignment="Center" 
                                            ContentSource="Icon"/>
                            <ContentPresenter x:Name="HeaderHost" 
                                            Grid.Column="1" 
                                            ContentSource="Header" 
                                            RecognizesAccessKey="True" 
                                            Margin="{TemplateBinding Padding}"/>
                            <ContentPresenter x:Name="InputGestureText" 
                                            Grid.Column="2" 
                                            Margin="8,0,8,0" 
                                            ContentSource="InputGestureText" 
                                            VerticalAlignment="Center"/>
                            <Path x:Name="SubMenuPath" 
                                  Grid.Column="3" 
                                  Fill="{TemplateBinding Foreground}" 
                                  Margin="4,0,0,0" 
                                  VerticalAlignment="Center" 
                                  Data="M0,0 L4,3.5 L0,7 Z"/>
                            <Popup x:Name="SubMenuPopup" 
                                   Placement="Right" 
                                   HorizontalOffset="-1" 
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                                   AllowsTransparency="True" 
                                   Focusable="False" 
                                   PopupAnimation="Fade">
                                <Border x:Name="SubMenuBorder" 
                                        Background="{StaticResource Carbon.Brush.Background.Primary}"
                                        BorderBrush="{StaticResource Carbon.Brush.Background.Tertiary}"
                                        BorderThickness="1">
                                    <ScrollViewer Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                                        <StackPanel IsItemsHost="True" 
                                                  KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Active}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Helper}"/>
                        </Trigger>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="SubMenuPath" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsSubmenuOpen" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Active}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Light Theme MenuItem Style -->
    <Style x:Key="Carbon.MenuItem.Light" TargetType="MenuItem" BasedOn="{StaticResource Carbon.MenuItem.Base}">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="8,6"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
                <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
                <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Dark Theme MenuItem Style -->
    <Style x:Key="Carbon.MenuItem.Dark" TargetType="MenuItem" BasedOn="{StaticResource Carbon.MenuItem.Base}">
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Active}"/>
                <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Interactive.Primary}"/>
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Active}"/>
                <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Interactive.Primary}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Default MenuItem Style -->
    <Style x:Key="Carbon.MenuItem" TargetType="MenuItem" BasedOn="{StaticResource Carbon.MenuItem.Base}"/>

    <!-- Window Style -->
    <Style x:Key="Carbon.Window" TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Primary}" />
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}" />
    </Style>

    <!-- ✨ NEW: Enhanced Color System -->
    <!-- Success Colors -->
    <SolidColorBrush x:Key="Carbon.Brush.Success.Primary" Color="#22C55E"/>
    <SolidColorBrush x:Key="Carbon.Brush.Success.Background" Color="#F0FDF4"/>
    <SolidColorBrush x:Key="Carbon.Brush.Success.Border" Color="#BBF7D0"/>

    <!-- Warning Colors -->
    <SolidColorBrush x:Key="Carbon.Brush.Warning.Primary" Color="#F59E0B"/>
    <SolidColorBrush x:Key="Carbon.Brush.Warning.Background" Color="#FFFBEB"/>
    <SolidColorBrush x:Key="Carbon.Brush.Warning.Border" Color="#FED7AA"/>

    <!-- Danger Colors -->
    <SolidColorBrush x:Key="Carbon.Brush.Danger.Primary" Color="#EF4444"/>
    <SolidColorBrush x:Key="Carbon.Brush.Danger.Background" Color="#FEF2F2"/>
    <SolidColorBrush x:Key="Carbon.Brush.Danger.Border" Color="#FECACA"/>

    <!-- Info Colors -->
    <SolidColorBrush x:Key="Carbon.Brush.Info.Primary" Color="#3B82F6"/>
    <SolidColorBrush x:Key="Carbon.Brush.Info.Background" Color="#EFF6FF"/>
    <SolidColorBrush x:Key="Carbon.Brush.Info.Border" Color="#BFDBFE"/>

    <!-- ✨ NEW: Modern Card Styles -->
    <Style x:Key="Carbon.Card" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="8" ShadowDepth="2"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Carbon.Card.Hover" TargetType="Border" BasedOn="{StaticResource Carbon.Card}">
        <Setter Property="Cursor" Value="Hand"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource Carbon.Brush.Info.Primary}"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="Black" Opacity="0.12" BlurRadius="10" ShadowDepth="3"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ✨ NEW: Subtle Card Hover (for Dashboard) -->
    <Style x:Key="Carbon.Card.Subtle" TargetType="Border" BasedOn="{StaticResource Carbon.Card}">
        <Setter Property="Cursor" Value="Hand"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource Carbon.Brush.Info.Primary}"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="Black" Opacity="0.12" BlurRadius="10" ShadowDepth="3"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ✨ NEW: Enhanced Button Styles -->
    <Style x:Key="Carbon.Button.Primary" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Base}">
        <Setter Property="Background" Value="{StaticResource Carbon.Brush.Info.Primary}"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#2563EB"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Carbon.Button.Success" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Base}">
        <Setter Property="Background" Value="{StaticResource Carbon.Brush.Success.Primary}"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#16A34A"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Carbon.Button.Warning" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Base}">
        <Setter Property="Background" Value="{StaticResource Carbon.Brush.Warning.Primary}"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#D97706"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Carbon.Button.Danger" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Base}">
        <Setter Property="Background" Value="{StaticResource Carbon.Brush.Danger.Primary}"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#DC2626"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ✨ NEW: Icon Button Styles -->
    <Style x:Key="Carbon.Button.Icon" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Secondary}">
        <Setter Property="Width" Value="36"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="MinWidth" Value="36"/>
    </Style>

    <Style x:Key="Carbon.Button.IconSmall" TargetType="Button" BasedOn="{StaticResource Carbon.Button.Icon}">
        <Setter Property="Width" Value="28"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="MinWidth" Value="28"/>
    </Style>

    <!-- ✨ NEW: Status Badge Styles -->
    <Style x:Key="Carbon.Badge.Success" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Carbon.Brush.Success.Primary}"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>

    <Style x:Key="Carbon.Badge.Warning" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Carbon.Brush.Warning.Primary}"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>

    <Style x:Key="Carbon.Badge.Danger" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Carbon.Brush.Danger.Primary}"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>

    <Style x:Key="Carbon.Badge.Info" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Carbon.Brush.Info.Primary}"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>

    <!-- ✨ NEW: Enhanced TextBox Styles -->
    <Style x:Key="Carbon.TextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <ScrollViewer x:Name="PART_ContentHost" 
                                     Padding="{TemplateBinding Padding}"
                                     VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource Carbon.Brush.Info.Primary}"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Interactive.Hover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ✨ NEW: Toolbar Styles -->
    <Style x:Key="Carbon.ToolBar" TargetType="ToolBar">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="8"/>
    </Style>

    <Style x:Key="Carbon.ToolBarTray" TargetType="ToolBarTray">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
    </Style>

    <!-- ✨ NEW: Enhanced ComboBox Styles -->
    <Style x:Key="Carbon.ComboBox" TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource Carbon.Brush.Background.Secondary}"/>
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Border" 
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition />
                                                <ColumnDefinition Width="20" />
                                            </Grid.ColumnDefinitions>
                                            <ContentPresenter Grid.Column="0"
                                                            Margin="8,6"
                                                            HorizontalAlignment="Left"
                                                            VerticalAlignment="Center" />
                                            <Path x:Name="Arrow"
                                                  Grid.Column="1"
                                                  Fill="{DynamicResource Carbon.Brush.Text.Primary}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Data="M0,0 L4,4 L8,0 Z"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Carbon.Brush.Interactive.Hover}"/>
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource Carbon.Brush.Info.Primary}"/>
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite"
                                        Margin="8,6"
                                        IsHitTestVisible="False"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                        ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                        <Popup x:Name="Popup"
                               AllowsTransparency="True"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                  SnapsToDevicePixels="True">
                                <Border x:Name="DropDownBorder"
                                        Background="{DynamicResource Carbon.Brush.Background.Secondary}"
                                        BorderBrush="{DynamicResource Carbon.Brush.Background.Tertiary}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Effect="{StaticResource Carbon.Shadow.Medium}"/>
                                <ScrollViewer SnapsToDevicePixels="True" Margin="1">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Carbon.ComboBoxItem" TargetType="ComboBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Carbon.Brush.Text.Primary}"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="4"
                            Margin="2">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Carbon.Brush.Background.Tertiary}"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Carbon.Brush.Interactive.Hover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary> 